# Los paquetes del workspace se enlazan entre si sin publicar en el registro.
link-workspace-packages=true
# Evita dependencias fantasma: un paquete solo importa lo que declara.
strict-peer-dependencies=false
auto-install-peers=true
